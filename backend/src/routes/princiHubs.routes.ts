import { Router } from 'express';

const router = Router();

// Placeholder PrinciHubs routes
router.get('/', (req, res) => {
  res.json({ message: 'PrinciHubs endpoint' });
});

export default router;
