import { Router } from 'express';

const router = Router();

// Placeholder PrinciFlash routes
router.get('/', (req, res) => {
  res.json({ message: 'PrinciFlash endpoint' });
});

export default router;
