import { Router } from 'express';

const router = Router();

// Placeholder PrinciCatalyst routes
router.get('/', (req, res) => {
  res.json({ message: 'PrinciCatalyst endpoint' });
});

export default router;
