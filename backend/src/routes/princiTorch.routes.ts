import { Router } from 'express';

const router = Router();

// Placeholder PrinciTorch routes
router.get('/', (req, res) => {
  res.json({ message: 'PrinciTorch endpoint' });
});

export default router;
