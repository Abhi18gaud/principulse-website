import { Router } from 'express';

const router = Router();

// Placeholder PrinciCircle routes
router.get('/', (req, res) => {
  res.json({ message: 'PrinciCircle endpoint' });
});

export default router;
